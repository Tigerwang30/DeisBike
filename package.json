{
  "name": "deisbikes",
  "version": "1.0.0",
  "description": "DeisBikes - Brandeis University Bike Share System",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run api\" \"npm run client\"",
    "api": "uvicorn api.index:app --reload --port 3001",
    "test:diagnose": "python tests/test_api_diagnosis.py",
    "client": "cd client && npm run dev",
    "setup": "npm install --prefix client && pip install -r requirements.txt",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
